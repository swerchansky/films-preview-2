ext {
    applicationId = "swerchansky.films.preview"
    compileSdkVersion = 33
    buildToolsVersion = "30.0.2"
    minSdkVersion = 24
    targetSdkVersion = 33
    versionCode = 1
    versionName = "1.0"
    androidCompileSdkVersion = 33
    testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"

    coreKtxVersion = '1.9.0'
    lifeCycleVersion = '2.6.2'
    activityComposeVersion = '1.8.0'

    junitVersion = '4.13.2'
    supportTestVersion = '1.1.5'
    espressoVersion = '3.5.1'

    appDependencies = [
            androidCoreKtx        : "androidx.core:core-ktx:$coreKtxVersion",
            androidLifecycle      : "androidx.lifecycle:lifecycle-runtime-ktx:$lifeCycleVersion",
            activityCompose      : "androidx.activity:activity-compose:$activityComposeVersion",
    ]

    appTestDependencies = [
            junit                 : "junit:junit:$junitVersion",
            supportJunit          : "androidx.test.ext:junit:$supportTestVersion",
            espresso              : "androidx.test.espresso:espresso-core:$espressoVersion"
    ]

    dataDependencies = [
            androidCoreKtx        : "androidx.core:core-ktx:$coreKtxVersion",
            androidLifecycle      : "androidx.lifecycle:lifecycle-runtime-ktx:$lifeCycleVersion",
    ]

    dataTestDependencies = [
            junit                 : "junit:junit:$junitVersion",
            supportJunit          : "androidx.test.ext:junit:$supportTestVersion",
            espresso              : "androidx.test.espresso:espresso-core:$espressoVersion"
    ]

    domainDependencies = [
            androidCoreKtx        : "androidx.core:core-ktx:$coreKtxVersion",
    ]

    domainTestDependencies = [
            junit                 : "junit:junit:$junitVersion",
    ]

    presentationDependencies = [
            androidCoreKtx        : "androidx.core:core-ktx:$coreKtxVersion",
    ]

    presentationTestDependencies = [
            junit                 : "junit:junit:$junitVersion",
            supportJunit          : "androidx.test.ext:junit:$supportTestVersion",
            espresso              : "androidx.test.espresso:espresso-core:$espressoVersion"
    ]
}